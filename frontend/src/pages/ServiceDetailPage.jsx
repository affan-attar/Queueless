import PagePlaceholder from '../components/PagePlaceholder'

export default function ServiceDetailPage() {
  return (
    <PagePlaceholder
      title="Organization Details"
      description="Organization profile, services, and live wait times, with Join Queue actions — powered by Phase 2/3 endpoints."
    />
  )
}
