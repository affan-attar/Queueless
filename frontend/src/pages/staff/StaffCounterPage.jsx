import PagePlaceholder from '../../components/PagePlaceholder'

export default function StaffCounterPage() {
  return (
    <PagePlaceholder
      title="Counter"
      description="Dedicated view for a single assigned counter — powered by the queues module (Phase 3)."
    />
  )
}
