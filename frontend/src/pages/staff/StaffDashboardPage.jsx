import PagePlaceholder from '../../components/PagePlaceholder'

export default function StaffDashboardPage() {
  return (
    <PagePlaceholder
      title="My Counter"
      description="Currently serving, next customer, and Call Next / Start / Complete / Skip / No-show actions — powered by the queues module (Phase 3)."
    />
  )
}
